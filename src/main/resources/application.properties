spring.application.name=greeting-app
# Feature flag: controls whether GreetingService increments the counter when creating a greeting
# Set to false to keep the counter unchanged on each call to createGreeting
greeting.counter.increment.enabled=true